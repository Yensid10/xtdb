.PHONY: all clean build run

# Build Docker image
build:

# Run the container
run:

all: build run

# Clean up
clean:
	docker rm -f xtdb-ssl 2>/dev/null || true
	docker rmi xtdb-ssl 2>/dev/null || true 